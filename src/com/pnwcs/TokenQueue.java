package com.pnwcs;

import java.util.Queue;

public class TokenQueue extends Queue<Token> {
    public void read(String arg) {
        // read file, one line at a time, breaking it down by whitespace initially
        // it will be further broken down in the Resolve Token step
    }
}
